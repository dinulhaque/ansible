---
- hosts: "{[ host }}"
  gather_facts: false
  connection: local
  vars_files:
    - /etc/ansible/vault/vault.yml
  tasks:
    - name: change hostname
      ios_command:
          commands: show version
      register: version
    - name: second task
      debug:
        var: version
